# Mychat

Monorepo with:
- `my-chat/` — Vite + React app
- `user/` — user-facing React app

## Dev
```bash
cd my-chat && npm i && npm run dev
# or
cd user && npm i && npm run dev
```

## Build
```bash
npm run build
```
# HimanChat - Real-time Messenger App
